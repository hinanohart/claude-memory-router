---
name: sandbox cluster
description: notes for the sandbox cluster orchestration helper
type: project
---

# Sandbox cluster

Used to test that "sandbox" + "cluster" together (two filename tokens)
trigger filename_boost >= 6 even without explicit aliases.
