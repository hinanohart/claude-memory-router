---
name: pinned rules (always injected)
description: example of a pinned memory file that the router always loads
type: reference
---

# Project rules

These rules are pinned via `CLAUDE_MEMORY_PIN=critical-rules.md` and
should be present in every router invocation regardless of the prompt.
